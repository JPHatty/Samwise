# Docker Compose Local Environment Template
# Copy to .env.local and customize for your environment

COMPOSE_PROJECT_NAME=samwise
COMPOSE_FILE=compose.yaml

# Service versions (override defaults)
# TRAEFIK_VERSION=v2.10
# REDIS_VERSION=7-alpine
# N8N_VERSION=latest
# LIVEKIT_VERSION=latest
